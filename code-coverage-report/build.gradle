plugins {
    id("account.jacoco-aggregation")
}

dependencies {
    implementation(project(":account-service"))
    implementation(project(":eventstore"))
    implementation(project(":eventstore-postgres"))
    implementation(project(":eventstore-mysql"))
}
